import React from 'react';

import './addMessage.scss';

function AddMessage() {
  return (
    <div className="add-message">
      <span>Repository added to your list</span>
    </div>
  );
}

export default AddMessage;
